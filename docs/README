
Documentation is available at http://nginx.org

#1、将打包好的前端文件放置在html文件夹
#2、将nginx.conf文件配置好，注意network访问路径是否有后缀
#3、mginx所在目录不准有中文，必要时需要在主目录下新建temp文件夹
#4、如果自己没有额外的配置，默认输入localhost即可打开首页
#5、nginx下面可以配置多个项目，直接将项目放在html的同级目录下，输入 start nginx，访问时需要在localhost后面加上‘/项目文件夹’
